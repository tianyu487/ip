/**
 * Contains the littleDaisy application entry point and top-level coordinator.
 * The coordinator delegates user interaction, parsing, persistence, and task
 * management to the packages dedicated to those responsibilities.
 */
package littledaisy;
